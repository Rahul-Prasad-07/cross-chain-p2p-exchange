version: "3.8"

services:
  db:
    image: postgres:16
    container_name: indexer-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: postgres
    ports:
      - "5432:5432"
    volumes:
      - ./sql:/sql   # ✅ mount your sql folder into the container
